public class test {
    public static void main(String[] args) {
        /*SLList slList = new SLList(5);
        slList.addFirst(6);
        slList.addLast(7);
        System.out.println(slList.size(slList.first));
*/
        SSListFinalVersion ssListFinalVersion = new SSListFinalVersion(5);
    }
}
